/**
 * @NApiVersion 2.1
 */
define([],

    () => {

        const helloFunction = (name) => {
            return `Hello ${name}`;
        }

        const bar = () => {

        }

        return {helloFunction, bar}

    });
